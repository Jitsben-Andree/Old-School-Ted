spring.application.name=OldSchoolTeed

# BASE DE DATOS
spring.datasource.url=jdbc:postgresql://localhost:5432/oldschoolteed_db
spring.datasource.username=postgres
spring.datasource.password=0102
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / HIBERNATE
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# SERVIDOR Y CONTEXTO
server.port=8080
server.servlet.context-path=/api/v1

# SEGURIDAD JWT
jwt.secret=VGhpcy1pcy1hLXN1cGVyLXNlY3VyZS1qd3Qtc2VjcmV0LTI1Ng==
jwt.expiration=3600000

# CONFIGURACIÓN DE IMÁGENES
file.upload-dir=./uploads
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# LOGGING
logging.level.org.springframework.security=DEBUG

# EMAIL
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=utpestudio0@gmail.com
spring.mail.password=ispt zdum fmri ixaw
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# --- MONITOREO (ACTUATOR) ---
management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always
management.metrics.tags.application=OldSchoolTees-Backend

# --- SENTRY
sentry.dsn=https://ff900c9464e22b55374d41231acfacb9@o4510462223646720.ingest.us.sentry.io/4510463630508032

# Enviar datos básicos del usuario (IP, Headers)
sentry.send-default-pii=true

# Tasa de muestreo (1.0 = 100% de errores, 0.2 = 20% recomendado para producción real)
sentry.traces-sample-rate=1.0

sentry.debug=true